specVersion: "v1beta"
serviceId: "to-do app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "to-do app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
